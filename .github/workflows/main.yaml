name: Test
on: pull_request
jobs:
  standard-checks:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2

      - name: Install packages
        run: npm ci
      - name: Run Tests
        run: npm run ci:test
